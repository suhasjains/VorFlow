import numpy as np


class Data:

        def __init__(self,N):
                self.u_vel = np.zeros(N);
                self.v_vel = np.zeros(N);
                self.press = np.zeros(N);



#def solve(...)

